@page "/PutAddress/{id:int}"
@model LogisticControl.Api.Pages.Company.Address.UpdateAddressModel
@{
    ViewData["Title"] = "Editar Endereço";
}

<h2>Editar Endereço</h2>

<form method="post">
    <div>
        <label for="inputStreet">Logradouro:</label>
        <input type="text" id="inputStreet" name="Street" value="@Model.Address.Street" required />
    </div>
    <div>
        <label for="inputNumber">Número:</label>
        <input type="number" id="inputNumber" name="Number" value="@Model.Address.Number" required />
    </div>
    <div>
        <label for="inputComplement">Complemento:</label>
        <input type="text" id="inputComplement" value="@Model.Address.Complement" name="Complement" />
    </div>
    <div>
        <label for="inputNeighborhood">Bairro:</label>
        <input type="text" id="inputNeighborhood" value="@Model.Address.Neighborhood" name="Neighborhood" required />
    </div>
    <div>
        <label for="inputCity">Cidade:</label>
        <input type="text" id="inputCity" value="@Model.Address.City" name="City" required />
    </div>
    <div>
        <label for="selectState">Estado:</label>
        <select id="selectState" name="State" required>
            @foreach (var state in Model.States)
            {
                <option value="@state" selected="@(state.ToString() == Model.Address.State ? "selected" : null)">@state.ToString()</option>
            }
        </select>
    </div>
    <div>
        <button type="submit">Editar</button>
    </div>
</form>
<a asp-page="/Company/Address/ViewCompany" asp-route-id="@Model.Address.CompanyId">
    <button>Voltar</button>
</a>